version: "3"

services:
  dashboard:
    build: .
    command: streamlit run main.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "8501:8501"
    volumes:
      - db_data:/app/data
    environment:
      - DB_PATH=/app/data/adhd_platform.db

volumes:
  db_data:
